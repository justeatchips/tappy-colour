{
  "name": "tappy-colour-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.5",
    "fake-indexeddb": "^6.2.5",
    "gh-pages": "^6.1.1",
    "jsdom": "^29.0.2",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "idb": "^8.0.0"
  }
}
