# Tappy Colour PWA — Technical Specification v1.0

See full spec output saved from Opus planning agent.
Stack: Vanilla TypeScript + Vite + IndexedDB + Service Worker. Lives in web/ subdirectory.
Deploy: GitHub Pages via `npm run deploy` from web/.

## Directory: web/
See PWA_SPEC.md (this file) and implementation in web/src/
